## External memory

`EXTMEM_DIR=/Users/kazuki.tanaka/devw/PRISM/external-intelligence`

For non-trivial debugging, investigation, architecture, or testing work, read
and follow `$EXTMEM_DIR/AGENTS.md`.

Run the CLI as `$EXTMEM_DIR/.venv/bin/extmem` and pass `--root $EXTMEM_DIR` to
every command so all repositories use the same store. Never record a memory
without human approval.